### CMake Configuration Directory

This directory contains CMake scripts and modules to support the project's build system.

#### Key Files

- `Dependencies.cmake`: Defines external dependencies for the project, including libraries like QuantLib, spdlog, fmt, and nlohmann/json. It handles their inclusion via vcpkg or other methods.
